'use client'

import { motion } from 'framer-motion'

export default function InvestorSection() {
  const partners = [
    'Google',
    'TBD',
    'Cerebros',
    'AshHQ',
    'Phoenix LiveView',
    'Elixir Foundation'
  ]

    // Removed for DIOS_Gate revamp
    // Removed for DIOS_Gate revamp
}